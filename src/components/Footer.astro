---
// Footer.astro - Simple footer with copyright and social links
---

<footer class="bg-dark-surface border-t border-white/5 mt-auto">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-12">
    <!-- Copyright -->
    <div class="text-center">
      <p class="text-text-secondary text-sm">
        &copy; {new Date().getFullYear()} John Ciresi. All rights reserved.
      </p>
    </div>
  </div>
</footer>

<style>
  .social-link {
    position: relative;
  }

  .social-link::before {
    content: '';
    position: absolute;
    inset: 0;
    border-radius: 9999px;
    padding: 1px;
    background: linear-gradient(135deg, rgba(0, 102, 204, 0.15), rgba(110, 110, 115, 0.15));
    -webkit-mask: linear-gradient(#fff 0 0) content-box, linear-gradient(#fff 0 0);
    -webkit-mask-composite: xor;
    mask-composite: exclude;
    opacity: 0;
    transition: opacity 0.3s;
  }

  .social-link:hover::before {
    opacity: 1;
  }
</style>
